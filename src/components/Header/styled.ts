import styled from 'styled-components';

export const HeaderWrapper = styled.div`
  text-align: center;
  font-size: 2em;
  font-weight: 500;

  display: flex;
  flex-direction: row;
  justify-content: center;
  align-items: center;
  height: 100px;

  p {
    padding: 0;
    margin: 0 0 0 8px;
  }
`;
